import { AppShell } from "@/components/layout/AppShell"; import { MeasurementList } from "@/components/measurements/MeasurementList"; import { createClient } from "@/lib/supabase/server"; import type { Measurement } from "@/types/database"; import Link from "next/link";
export default async function Page(){const s=await createClient();const {data}=await s.from("blood_pressure_records").select("*").order("measured_at",{ascending:false});return <AppShell><div className="mx-auto max-w-5xl p-5 md:p-8"><div className="mb-7 flex items-end justify-between"><div><p className="text-sm font-bold text-cyan-700">NHẬT KÝ</p><h1 className="mt-1 text-3xl font-black">Lịch sử đo</h1></div><Link href="/measurements/new" className="btn btn-primary">+ Thêm lần đo</Link></div><MeasurementList initial={(data||[]) as Measurement[]}/></div></AppShell>}
