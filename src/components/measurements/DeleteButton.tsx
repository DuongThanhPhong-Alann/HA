"use client";import { createClient } from "@/lib/supabase/client";import { useRouter } from "next/navigation";import { toast } from "sonner";import { Trash2 } from "lucide-react";
export function DeleteButton({id,path}:{id:string;path:string|null}){const router=useRouter();return <button className="btn border border-red-200 text-red-700" onClick={async()=>{if(!confirm("Bạn chắc chắn muốn xóa lần đo này?"))return;const s=createClient();const {error}=await s.from("blood_pressure_records").delete().eq("id",id);if(error)return toast.error(error.message);if(path)await s.storage.from("bp-images").remove([path]);toast.success("Đã xóa");router.push("/measurements");router.refresh()}}><Trash2 size={18}/>Xóa</button>}
