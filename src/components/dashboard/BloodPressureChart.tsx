"use client"; import { useState } from "react"; import type { Measurement } from "@/types/database"; import { LineChart,Line,XAxis,YAxis,CartesianGrid,Tooltip,ResponsiveContainer,Legend } from "recharts"; import { format,subDays } from "date-fns";
export function BloodPressureChart({records}:{records:Measurement[]}){const [days,setDays]=useState("30");const cutoff=days==="all"?0:subDays(new Date(),Number(days)).getTime();const data=records.filter(x=>new Date(x.measured_at).getTime()>=cutoff).slice().reverse().map(x=>({...x,date:format(new Date(x.measured_at),"dd/MM")}));return <div className="card p-5"><div className="mb-5 flex items-center justify-between"><h2 className="font-extrabold">Xu hướng chỉ số</h2><select className="rounded-lg border p-2 text-sm" value={days} onChange={e=>setDays(e.target.value)}><option value="7">7 ngày</option><option value="30">30 ngày</option><option value="90">90 ngày</option><option value="all">Tất cả</option></select></div><div className="h-72">{data.length?<ResponsiveContainer width="100%" height="100%"><LineChart data={data}><CartesianGrid strokeDasharray="3 3" vertical={false}/><XAxis dataKey="date" fontSize={12}/><YAxis fontSize={12}/><Tooltip/><Legend/><Line type="monotone" dataKey="systolic" name="SYS" stroke="#dc2626" strokeWidth={2}/><Line type="monotone" dataKey="diastolic" name="DIA" stroke="#176b87" strokeWidth={2}/><Line type="monotone" dataKey="pulse" name="PULSE" stroke="#d97706" strokeWidth={2}/></LineChart></ResponsiveContainer>:<div className="grid h-full place-items-center text-slate-400">Chưa có đủ dữ liệu để hiển thị</div>}</div></div>}
